# Type Check

Run TypeScript type checking and fix any type errors.

## Steps

1. Run `cd app && npm run typecheck`
2. If there are errors:
   - Read the error output carefully
   - Fix each type error in the source files
   - Re-run `cd app && npm run typecheck`
3. Repeat until type checking passes with zero errors
4. Report the final typecheck status
